# 从没碰过代码？照这个做就行

这份教程写给**完全不懂编程的人**。你不需要看懂任何代码——动手干活的是 AI，你只负责几个 AI 替你做不了的动作：注册账号、点授权按钮、偶尔复制粘贴一行字。全程免费，不需要信用卡。

最终你会得到一个 `https://你的项目名.pages.dev` 的网址：自己能打开，发给朋友、朋友在手机上也能打开。

---

## 第 0 步：准备（一次性的，10 分钟）

### 0.1 你手里要有一个做好的网页

就是你让 AI（Claude、ChatGPT 等）帮你写的那个东西——可能是电脑里的一个文件夹，也可能是一个 GitHub 链接。知道它在哪就行。

### 0.2 安装 Node（AI 干活要用的工具箱）

1. 浏览器打开 **nodejs.org**
2. 点页面中间那个**绿色大按钮**（写着 LTS 的）下载
3. 双击下载好的安装包，一路点"继续 / 下一步 / 同意"，直到"完成"

### 0.3 注册 Cloudflare 账号（你网站住的免费房子）

1. 浏览器打开 **dash.cloudflare.com/sign-up**
2. 填邮箱、设密码，注册
3. 去你的邮箱收验证邮件，点里面的确认链接

不需要绑卡，免费版没有时间限制，不是试用。

---

## 认识一个词：终端（Terminal）

后面会反复出现"在终端里跑这条命令"。**终端就是一个能给电脑下文字指令的窗口**，你只需要会三个动作：打开它、粘贴、按回车。

**Mac 打开方法**：按 `Command + 空格`，输入 `终端`（或 Terminal），按回车。
**Windows 打开方法**：点开始菜单，输入 `PowerShell`，按回车。

打开后是一个窗口，有一行一闪一闪的光标——它在等你输入，不用怕它。

顺手验证一下第 0 步装好了没：把下面这行**复制**，在终端窗口里**粘贴**，按**回车**：

```
node -v
```

如果显示 `v20` 或 `v22` 之类的数字，说明装好了。显示"找不到命令"就回去重做 0.2。

---

## 正式开始：三步上线

### 第 1 步：把手册交给你的 AI

这个仓库里有一份写给 AI 看的手册 [`PORTABLE.md`](PORTABLE.md)。你要做的是把它交给一个**能在你电脑上干活的 AI**：

- ✅ 能用的：Claude Code、Cursor、Windsurf、Codex CLI 这类（判断标准一句话：**它能替你在电脑上执行命令**）
- ❌ 不能用的：网页版 ChatGPT / 网页版 Claude 这类纯聊天窗口（它们只能说，不能动手）

做法：打开 [`PORTABLE.md`](PORTABLE.md)，把内容全选复制发给 AI（或者直接给 AI 这个链接），然后说：

> 照这份手册，把我的网页上线。我的项目在（文件夹位置 / GitHub 链接）。我不懂代码，每一步请用大白话解释。

### 第 2 步：回答 AI 的两三个问题

AI 开工前会停下来问你几个问题。对照翻译：

| AI 问的 | 人话翻译 | 不知道选什么就 |
|---|---|---|
| "要到哪一层？L0/L1/L2/L3" | 你的网站要不要**记住用户的东西**？只是给人看 → L0；要登录、要保存数据 → 把需求告诉它让它推荐 | 选 **L0**（以后随时能升级） |
| "CLI 直推还是 GitHub 集成？" | 手动发布还是自动发布 | 选 **CLI 直推**（简单） |
| "仓库要设成公开吗？" | 你的代码要不要给全世界看 | 不确定就**不公开**，选别的路线 |

### 第 3 步：AI 停下来找你时，照做

AI 干到某几步会说"这步需要你自己做"。总共就三类，都不难：

**A 类：浏览器点授权**
AI 会让你在终端跑一行类似 `npx wrangler login` 的命令。做法：复制那行字 → 粘贴到终端 → 回车 → 会**自动弹出浏览器页面** → 点"允许 / Allow" → 回到 AI 那里说"好了"。

**B 类：在终端跑命令**
AI 给你一条命令让你自己跑（一般是它的环境没权限）。做法：复制 → 粘贴到终端 → 回车 → 等它跑完（屏幕滚一堆字是正常的，说明在干活）→ 把**最后几行**结果复制回给 AI。一次只跑一条。

**C 类：去某个网页点几下**
比如注册账号、在管理面板点个按钮。AI 会给你网址和步骤，照着点完回来说"好了"。

---

## 怎么知道成功了

AI 最后会给你一个网址，长这样：

```
https://你的项目名.pages.dev
```

三个动作确认收工：

1. 自己电脑上打开它
2. **用手机也打开一次**（手机不用连什么特殊网络，就普通地开）
3. 发给一个朋友，让 ta 打开看看

三个都能开 = 你的网站真的上线了。这个网址永久有效，不花钱。

---

## 会吓到你、但其实没事的情况

| 你看到的 | 实际含义 | 你要做的 |
|---|---|---|
| 终端滚出一大屏字 | 工具在正常干活 | 等它停 |
| 红色的 ERROR | 出错了，但错误信息就是修复线索 | 整段复制发给 AI，别自己猜 |
| 刚上线的网址打不开，提示 SSL/安全连接错误 | 你开的可能是带一串乱码前缀的长网址 | 用**不带乱码前缀**的短网址（`项目名.pages.dev`）；还不行就等几分钟再试 |
| AI 说"被沙盒拦住了，请你在终端执行" | AI 的工作环境没权限，需要借你的手 | 按上面 B 类照做 |
| 网站在国内打开时快时慢 | pages.dev 在中国大陆访问不稳定，正常现象 | 自用没事；主要给国内用户用的话问 AI 长期方案 |

**小词典**（每个一句话）：
- **终端** = 给电脑下文字指令的窗口
- **命令** = 你粘贴进终端的那行字
- **构建（build）** = 把项目源文件打包成浏览器能直接用的形式
- **部署（deploy）** = 把打包好的文件上传到服务器
- **URL** = 网址

---

## 三条安全提醒（比教程本身重要）

1. **AI 建议你把仓库"改成公开（public）"时，停下来想清楚**：公开 = 你的代码全世界可见，而且被人复制走就收不回。不理解为什么要公开，就回它"不公开，换个方式"。
2. **任何叫"密钥 / token / API key"的东西，不要发给 AI，也不要发到群里**。正规流程里 AI 永远不需要你把密钥内容发给它。
3. **不确定 AI 让你做的事是干嘛的，先问一句"这步是干什么的？会有什么后果？"再动手。** 好的 AI 会解释，解释不清就别做。

---

有问题？把你卡住的那一步和屏幕上的原话，原样发给你的 AI——描述得越原样，修得越快。
